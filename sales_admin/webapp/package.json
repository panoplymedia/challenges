{
  "name": "webapp",
  "version": "1.0.0",
  "description": "A Sales Admin applicaiton",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "start": "concurrently \"npm run start --prefix server\" \"npm run start --prefix client\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.1"
  }
}
