import { customerCollection } from "./customer";

describe("Customer Collection", () => {
  describe("ensureCustomerCollection", () => {
    xit("returns a customer collection", () ={});
  });
  describe("createCustomer", () => {
    xit("adds a customer to the database", () => {});
    xit("throws an error on invalid entry", () => {});
  });
});
