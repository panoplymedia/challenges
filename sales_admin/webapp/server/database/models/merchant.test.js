import { merchantCollection } from "./merchant";

describe("Merchant Collection", () => {
  describe("ensureMerchantCollection", () => {
    xit("returns a merchant collection", () ={});
  });
  describe("createmerchant", () => {
    xit("adds a merchant to the database", () => {});
    xit("throws an error on invalid entry", () => {});
  });
});
