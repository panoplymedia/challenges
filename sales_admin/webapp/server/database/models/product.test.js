import { productCollection } from "./product";

describe("Product Collection", () => {
  describe("ensureProductCollection", () => {
    xit("returns a product collection", () ={});
  });
  describe("createProduct", () => {
    xit("adds a product to the database", () => {});
    xit("throws an error on invalid entry", () => {});
  });
});
