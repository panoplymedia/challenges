describe("mapRoutes", () => {
  xit("adds root level routes to app", () => {});
  xit("adds nested routes to app", () => {});
  xit("ignores routes that are not an object or function", () => {});
});
